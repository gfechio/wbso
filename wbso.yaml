# Issue tracker configuration
issue_tracker:
  type: youtrack
  config:
    base_url: https://your-jira-instance.com
    token: your-jira-token
    project_id: YOUR_PROJECT_ID

time_tracker:
  type: file
  path: /home/.wbso/time.json

